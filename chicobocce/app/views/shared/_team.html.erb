<% @teams.each do |team| %>
  <div>
    <table width="60%">
      <tr>
        <td><%= team.name %> :</td>
        <td class="teamhome"><%= team.competes.sum(:score) %></td>
      </tr>
</table>
</div>
<% end %>


